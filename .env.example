# Cookie-signed auth (dev defaults match the demo login in README)
AUTH_USERNAME=scott
AUTH_PASSWORD=buildinator
AUTH_SECRET=dev-only-change-me-please-use-32b!!

# mock (default) | remote (stub — not implemented in v0)
GROK_ADAPTER=mock

# Used by RemoteGrokAdapter in v1. ACP/HTTP host that speaks session/new, session/load, session/prompt.
# GROK_REMOTE_URL=http://127.0.0.1:8080
